{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm build",
  "outputDirectory": "dist/web/browser",
  "framework": null,

  "rewrites": [
    {
      "//": "THE ONE LINE YOU MUST EDIT: point this at your Railway API.",
      "source": "/api/:path*",
      "destination": "https://REPLACE-WITH-YOUR-APP.up.railway.app/api/:path*"
    },
    {
      "//": "SPA fallback. Vercel matches real files first, so this only catches client-side routes like /colecao, which would otherwise 404 on reload. Must come after the /api rule — rewrites are evaluated in order.",
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],

  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "Content-Security-Policy",
          "value": "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data: https://storage.googleapis.com; font-src 'self'; connect-src 'self'; frame-ancestors 'none'; base-uri 'self'; form-action 'self'; object-src 'none'"
        },
        { "key": "X-Content-Type-Options", "value": "nosniff" },
        { "key": "X-Frame-Options", "value": "DENY" },
        { "key": "Referrer-Policy", "value": "no-referrer" },
        { "key": "Strict-Transport-Security", "value": "max-age=31536000; includeSubDomains" },
        { "key": "Permissions-Policy", "value": "camera=(), microphone=(), geolocation=(), interest-cohort=()" }
      ]
    },
    {
      "//": "Angular fingerprints these filenames, so a change is a new URL — they can be cached forever.",
      "source": "/(.*)\\.(js|css|woff2|woff|ttf)",
      "headers": [{ "key": "Cache-Control", "value": "public, max-age=31536000, immutable" }]
    },
    {
      "//": "index.html is NOT fingerprinted: it's what points at the current bundle, so it must always be revalidated or users get pinned to a dead build.",
      "source": "/index.html",
      "headers": [{ "key": "Cache-Control", "value": "public, max-age=0, must-revalidate" }]
    }
  ]
}
